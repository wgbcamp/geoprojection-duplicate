{
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --mode production --host"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "type": "module",
  "dependencies": {
    "d3": "^7.9.0"
  }
}
